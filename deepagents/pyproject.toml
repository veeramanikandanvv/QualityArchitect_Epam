[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "quality-architect-deepagents"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "deepagents>=0.2.0",
  "langchain-openai>=0.3.0",
  "python-dotenv>=1.0.1",
  "requests>=2.32.0",
]

[project.scripts]
qa-assess = "qa_agents.main:main"

[tool.setuptools.packages.find]
where = ["src"]
