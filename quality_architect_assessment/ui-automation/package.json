{
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/parts.spec.ts",
    "test:api": "playwright test tests/parts-api.spec.ts",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
